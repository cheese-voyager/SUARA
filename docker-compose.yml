version: '3.8'

services:
  suara-frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      - suara_ecsfe
    restart: unless-stopped

  suara-backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    restart: unless-stopped
